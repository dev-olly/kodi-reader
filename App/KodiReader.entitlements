<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Sparkle's installer connection for sandboxed development builds. -->
	<key>com.apple.security.temporary-exception.mach-lookup.global-name</key>
	<array>
		<string>$(PRODUCT_BUNDLE_IDENTIFIER)-spks</string>
		<string>$(PRODUCT_BUNDLE_IDENTIFIER)-spki</string>
	</array>
	<!-- Sparkle ships signed helper code; ad-hoc Debug builds cannot validate its team. -->
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
	<key>com.apple.security.app-sandbox</key>
	<true/>
	<!-- WKWebView needs this to launch inside the sandbox. Live webpage
	     browsing and article-image downloads also use the network. -->
	<key>com.apple.security.network.client</key>
	<true/>
	<!-- Documents are opened through the open panel; the security-scoped bookmark
	     saved alongside each one is what allows reopening it on next launch. -->
	<key>com.apple.security.files.user-selected.read-only</key>
	<true/>
	<key>com.apple.security.files.bookmarks.app-scope</key>
	<true/>
	<!-- v0.1.0 only: read the Folio-era container and the unsigned Debug leak
	     so Recents and drawings can be copied once. -->
	<key>com.apple.security.temporary-exception.files.home-relative-path.read-only</key>
	<array>
		<string>/Library/Containers/com.olly.Folio/</string>
		<string>/Library/Application Support/EpubReader/</string>
	</array>
	<!-- Lets this build read generic-password items stored under the old bundle. -->
	<key>keychain-access-groups</key>
	<array>
		<string>$(AppIdentifierPrefix)com.olly.Folio</string>
		<string>$(AppIdentifierPrefix)com.olly.KodiReader</string>
	</array>
</dict>
</plist>
